# For this command I have figured out the - 
# a. Count of the movies; b. Average rental rate; c. Maximum rental duration and minimum rental duration
# Commands used - COUNT, AVG, MAX, MIN, WHERE, IN, GROUP BY

SELECT rating,
COUNT(film_id) AS count_of_movies,
AVG(rental_rate) AS Average_movie_rental_rate,
MAX(rental_duration) AS maximum_rental_duration,
MIN(rental_duration) AS minimum_rental_duration
FROM film
WHERE rating IN ('PG', 
		 'G')
GROUP BY rating;
